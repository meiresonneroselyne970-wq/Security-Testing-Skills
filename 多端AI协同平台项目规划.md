# 多端 AI 协同平台项目规划（精简版）

## 1. 项目定位

本项目定位为一个面向**多人、多设备、多应用 AI 助手**的协同平台。平台以聊天室为统一交互入口，将真实用户、手机、电视、智能穿戴设备、业务应用服务和 AI 助手纳入同一协同空间中。

聊天室不只是普通聊天工具，还承担以下能力：

- 用户之间的交流；
- 用户向设备、AI 或业务应用发布指令；
- AI 助手根据业务场景自动生成任务、提醒或内容卡片；
- 手机、电视等设备同步展示任务状态和执行结果；
- 多个应用能力通过统一会话空间串联起来，实现精准服务。

平台早期以“手机—电视互联”为基础，包括扫码绑定、状态同步、文件/文字/音视频上传、电视端接收展示等能力；后续逐步扩展为多端 AI 协同平台。

---

## 2. 已有基础能力

当前已具备的基础能力主要包括：

1. **手机与电视绑定**
   - TV 端创建二维码；
   - 手机端扫码并确认登录；
   - TV 端轮询二维码状态；
   - 绑定成功后同步用户昵称、手机号等信息；
   - 两端本地持久化绑定状态。

2. **手机向电视传输内容**
   - 支持文字、语音、视频、图片、文件等快捷发送；
   - TV 端可接收、展示或播放对应内容；
   - 传输内容可作为后续聊天室消息、任务卡片、资源卡片的基础。

3. **多设备扩展基础**
   - 当前以手机和电视为主；
   - 后续可扩展到智能手表、智能眼镜、大屏设备、应用 AI 助手和后端服务节点。

---

## 3. 核心业务思路

### 3.1 聊天室作为统一协同空间

系统中的“聊天室”可以理解为一个业务协同空间。一个聊天室可以对应：

- 一个班级群；
- 一个家庭空间；
- 一个医疗随访群；
- 一个会议协作空间；
- 一个设备协同空间；
- 一个围绕某项任务创建的临时会话。

聊天室中的参与者不仅包括用户，也可以包括设备、AI 助手和应用服务。

### 3.2 用户、设备、AI 助手统一参与协同

平台中需要支持以下参与者：

- 真实用户：老师、学生、医生、患者、家长、普通用户等；
- 物理设备：手机、电视、手表、眼镜等；
- AI 助手：教育 AI、医疗 AI、口语 AI、文件助手等；
- 应用服务：公司已有业务 App 对应的服务能力。

用户可以在聊天室中发普通消息，也可以发布任务指令。AI 助手可以接收指令，也可以根据业务规则主动发送提醒、任务或卡片。

### 3.3 精准服务

平台根据以下维度实现精准服务：

- 用户身份：老师、学生、医生、患者等；
- 群组场景：班级群、医疗群、家庭群、会议群等；
- 设备类型：手机、电视、手表、眼镜等；
- 应用能力：教育、医疗、口语、文件、会议等；
- AI 助手能力：总结、布置作业、服药提醒、口语练习、内容生成等。

例如：

- 在班级群中，老师总结教学内容后，教育 AI 生成作业，并从对象存储中获取作业 HTML 页面，以卡片形式发送到聊天室；
- 在医疗场景中，医疗 AI 根据患者任务提醒按时吃药，并在手机或电视端弹窗展示；
- 在口语练习场景中，手机负责录音与输入，电视负责展示 AI 对话、评分和建议。

---

## 4. 数据库设计总体思路

现有数据库已经具备 Hyperedge（超边）模型、用户、节点用户、Act、Episode、Metadata 等基础结构。新的多端 AI 协同平台不应完全推翻已有设计，而应在原有模型上进行语义扩展。

核心思想如下：

- `_users` 继续作为用户主体表；
- `_hyperedges` 可映射为聊天室、群组或业务协同空间；
- `_node_user_on_hyperedge` 可扩展为用户在某个协同空间中的角色关系；
- `_acts` 可表示业务动作、AI 能力或应用能力；
- `_themes` 可表示任务模板、内容模板或卡片模板；
- `_episodes` 可表示发布到某个协同空间中的任务流程或业务编排；
- metadata 表用于保存动态配置、标签、权限、能力参数等扩展信息；
- 新增“协同节点”和“用户节点绑定关系”以支持广义设备、AI 助手和应用服务。

---

## 5. 现有表的复用与语义映射

| 已有表 | 原有含义 | 在本项目中的建议定位 |
|---|---|---|
| `_users` | 系统用户 | 平台用户主体，保存基础用户信息、头像、邮箱、认证关联等 |
| `_hyperedges` | 超边结构 | 聊天室、班级群、医疗群、家庭空间、任务协同空间 |
| `_node_user_on_hyperedge` | 超边上的节点用户 | 用户在某个聊天室/协同空间中的角色、权限、进度与即时数据映射 |
| `_themes` | Act 处理的数据模板 | 作业模板、提醒模板、卡片模板、AI 输入模板 |
| `_acts` | 可执行动作 | AI 能力、应用能力、业务动作，如布置作业、生成总结、服药提醒 |
| `_edit_episodes` | 可编辑 Episode | 任务流程草稿、课程流程、提醒流程、AI 编排草稿 |
| `_episodes` | 发布到超边的 Episode | 发布到某个聊天室/群组的任务流程或业务活动 |
| `_hyperedge_metadata` | 超边元数据 | 聊天室类型、业务场景、访问规则、能力标签 |
| `_act_metadata` | Act 元数据 | AI 能力参数、输入输出约束、适用角色、调用配置 |
| `_episode_metadata` | Episode 元数据 | 任务流程参数、开始结束时间、权限、完成规则 |

---

## 6. 建议新增或扩展的核心表

### 6.1 协同节点表：`_collaboration_nodes`

用于统一登记所有可参与协同的对象，包括物理设备、AI 助手和应用服务。

| 字段 | 说明 |
|---|---|
| `id` | 节点主键 |
| `node_name` | 节点名称，如“客厅电视”“教育 AI 助手” |
| `node_type` | 节点类型：`USER_DEVICE`、`AI_AGENT`、`APP_SERVICE`、`SYSTEM_SERVICE` |
| `node_sub_type` | 子类型：`PHONE`、`TV`、`WATCH`、`GLASSES`、`EDU_AI`、`MEDICAL_AI` 等 |
| `owner_user_id` | 归属用户，可为空 |
| `app_id` | 所属应用标识，可用于区分教育、医疗、口语等 App |
| `device_identifier` | 设备唯一标识，物理设备使用 |
| `capability_tags` | 能力标签，如 display、record、notify、ai_generate |
| `online_status` | 在线状态 |
| `config_json` | 扩展配置 |
| `status` | 启用状态 |
| `created_at / updated_at` | 创建与更新时间 |

> 设计说明：这里的“节点”是广义概念，既包括手机、电视等物理设备，也包括 AI 助手和应用服务。这样可以避免未来接入新设备或新 AI 时重复建模。

---

### 6.2 用户与节点绑定表：`_user_node_bindings`

用于表示一个用户可以绑定多个设备或 AI 节点，一个节点也可以被多个用户共享。

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `user_id` | 用户 ID |
| `node_id` | 协同节点 ID |
| `bind_role` | 绑定角色：owner、member、controller、viewer |
| `bind_status` | 绑定状态：active、inactive、unbound |
| `is_default` | 是否默认设备 |
| `bind_time` | 绑定时间 |
| `last_active_time` | 最近活跃时间 |
| `created_at / updated_at` | 创建与更新时间 |

> 设计说明：该表是“用户—设备/节点”的多对多关系表，是设备调度、消息投递和精准服务的基础。

---

### 6.3 聊天消息表：`_chat_messages`

用于保存聊天室内的普通消息、指令消息、AI 回复、系统事件和卡片消息。

| 字段 | 说明 |
|---|---|
| `id` | 消息主键 |
| `hyperedge_id` | 所属聊天室/协同空间 |
| `sender_type` | 发送者类型：USER、DEVICE、AI_AGENT、SYSTEM |
| `sender_id` | 发送者 ID，可关联用户或协同节点 |
| `message_type` | 消息类型：TEXT、COMMAND、CARD、FILE、SYSTEM、AI_REPLY |
| `content` | 文本内容 |
| `intent_type` | 识别出的指令意图 |
| `target_node_id` | 目标节点，可为空 |
| `card_id` | 关联卡片 ID |
| `task_id` | 关联任务 ID |
| `created_at` | 发送时间 |

> 设计说明：聊天室消息不只是文本，还可能是指令、卡片、资源、AI 回复或系统事件。

---

### 6.4 消息卡片表：`_message_cards`

用于保存作业卡片、服药提醒卡片、口语练习卡片、文件卡片等结构化内容。

| 字段 | 说明 |
|---|---|
| `id` | 卡片主键 |
| `card_type` | 卡片类型：HOMEWORK、MEDICATION、ORAL_PRACTICE、FILE、NOTICE |
| `title` | 卡片标题 |
| `summary` | 卡片摘要 |
| `resource_type` | 资源类型：HTML、PDF、IMAGE、VIDEO、AUDIO、JSON |
| `resource_url` | COS 或其它存储中的资源地址 |
| `payload_json` | 卡片业务数据 |
| `created_by` | 创建者，可为用户或 AI 节点 |
| `created_at` | 创建时间 |

> 设计说明：AI 生成的作业、提醒、练习结果等不应只作为文本保存，而应以结构化卡片形式进入聊天室。

---

### 6.5 任务实例表：`_task_instances`

用于记录由用户或 AI 发起的任务执行过程。

| 字段 | 说明 |
|---|---|
| `id` | 任务主键 |
| `hyperedge_id` | 所属协同空间 |
| `source_message_id` | 触发任务的消息 |
| `act_id` | 对应的业务动作或 AI 能力 |
| `requester_user_id` | 发起用户 |
| `executor_node_id` | 执行节点，如教育 AI、医疗 AI |
| `task_status` | pending、running、success、failed、cancelled |
| `input_json` | 输入参数 |
| `output_json` | 输出结果 |
| `created_at / finished_at` | 创建与完成时间 |

> 设计说明：用户在聊天室中发布的指令需要被转化为任务实例，方便跟踪执行状态与结果。

---

### 6.6 任务投递表：`_task_deliveries`

用于记录某个任务或卡片投递到了哪些用户和设备。

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `task_id` | 任务 ID |
| `target_user_id` | 目标用户 |
| `target_node_id` | 目标设备或 AI 节点 |
| `delivery_status` | pending、delivered、read、confirmed、failed |
| `display_mode` | chat、popup、fullscreen、notification |
| `read_at` | 阅读时间 |
| `confirmed_at` | 确认时间 |

> 设计说明：同一个任务可能要发送到多个用户和多个设备，例如作业发给全班学生，服药提醒同时发送到手机和电视。

---

## 7. 与现有 Hyperedge 模型的关系

在本项目中，可以将 Hyperedge 理解为“业务协同空间”。例如：

- 班级群是一个 Hyperedge；
- 医疗随访群是一个 Hyperedge；
- 家庭设备空间是一个 Hyperedge；
- 一次口语练习会话也可以作为一个 Hyperedge 或 Episode。

`Act`、`Theme`、`Episode` 可以用于表达 AI 或应用服务的业务逻辑：

- `Act`：具体能力，如生成作业、服药提醒、创建口语练习；
- `Theme`：输入模板或内容模板；
- `Episode`：一组 Act 的编排与发布；
- `Metadata`：补充角色、权限、业务规则和 AI 参数。

这样可以复用已有数据模型，并将其扩展到多端 AI 协同场景。

---

## 8. 典型业务场景

### 8.1 教育场景

1. 老师在班级群中发送“总结今天课程并布置作业”；
2. 平台识别为教育任务，调用教育 AI 助手；
3. 教育 AI 根据课程内容生成作业；
4. 作业 HTML 页面存储在 COS 桶中；
5. AI 以作业卡片形式发送到班级聊天室；
6. 学生手机端收到提醒，电视端可展示作业卡片。

### 8.2 医疗场景

1. 医疗 AI 根据患者任务计划生成服药提醒；
2. 提醒消息进入医疗聊天室；
3. 系统根据用户绑定设备选择手机、电视或手表投递；
4. 手机端通知，电视端弹窗；
5. 用户确认后写入任务投递状态。

### 8.3 口语练习场景

1. 用户在手机端发起口语练习；
2. 炎图口语 AI 生成练习主题和对话内容；
3. 手机端负责录音和输入；
4. 电视端展示 AI 对话、用户回答、评分和建议；
5. 练习结果以卡片形式进入聊天室。

---

## 9. 阶段性建设规划

### 第一阶段：稳定手机—电视互联底座

- 完善手机与电视绑定；
- 完善文件、文字、音视频传输；
- 完善设备状态持久化；
- 形成设备注册与用户绑定的基础数据。

### 第二阶段：引入协同节点模型

- 新增协同节点表；
- 将手机、电视、AI 助手、应用服务统一登记；
- 建立用户与协同节点的多对多绑定关系。

### 第三阶段：建设聊天室能力

- 基于 `_hyperedges` 建立聊天室/协同空间；
- 支持普通消息、系统消息、指令消息；
- 支持用户、设备、AI 作为参与者。

### 第四阶段：接入 AI 助手与应用能力

- 将教育、医疗、口语等应用能力注册为 Act 或能力节点；
- 支持 AI 接收聊天室指令；
- 支持生成任务、卡片和提醒。

### 第五阶段：实现精准服务分发

- 根据用户身份、聊天室场景、设备能力进行任务投递；
- 支持手机、电视、手表等多端展示；
- 支持任务查看、确认、完成状态跟踪。

---

## 10. 设计注意点

1. **不要将设备表局限为物理设备**  
   设备应升级为“协同节点”，统一支持物理终端、AI 助手和应用服务。

2. **聊天室成员不能只支持用户**  
   后续 AI、设备、系统服务都可能作为成员参与会话。

3. **消息不能只设计为文本**  
   需要支持指令、卡片、文件、AI 回复和系统事件。

4. **任务需要可追踪**  
   用户发出的指令应形成任务实例，记录执行状态和结果。

5. **资源需要结构化管理**  
   作业 HTML、文件、图片、视频等应通过资源 URL 与卡片绑定。

6. **身份应与业务场景关联**  
   同一用户在不同群组中可能是老师、学生、医生、患者或家长。

7. **精准服务依赖设备能力**  
   手机适合输入和通知，电视适合展示和播放，手表适合轻提醒。

---

## 11. 简要结论

本项目应以现有手机—电视互联能力为基础，逐步升级为多端 AI 协同平台。数据库设计上，应复用已有 Hyperedge、Act、Episode、Metadata 等模型，并新增协同节点、用户节点绑定、聊天室消息、消息卡片和任务投递等表。这样既能承接当前手机与电视绑定、内容上传和大屏展示能力，也能为后续教育、医疗、口语等多应用 AI 助手接入提供统一的数据基础。
